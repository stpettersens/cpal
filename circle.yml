machine:
  ruby:
    version: 2.4.0

dependencies:
  pre:
    - git clone https://gist.github.com/18ebf6fe8d3f026d3e6987349880bd2a.git 
    - cd 18ebf6fe8d3f026d3e6987349880bd2a
    - sh rustup-init.sh -y
    - cd ..
    - ruby --version
    - rake --version
    - rust --version
    - cargo --version

test:
  override:
    - ruby --version
